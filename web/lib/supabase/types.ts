/**
 * Generated Supabase Database types placeholder.
 *
 * TODO: Regenerate with
 *   supabase gen types typescript --project-id <project-ref> --schema public > lib/supabase/types.ts
 * once `pasar-backend` has applied the Phase 0 schema migrations.
 *
 * Until then, callers should treat the client as `SupabaseClient<any>` and the
 * Database type below is intentionally minimal.
 */
export type Database = {
  public: {
    Tables: Record<string, unknown>;
    Views: Record<string, unknown>;
    Functions: Record<string, unknown>;
    Enums: Record<string, unknown>;
  };
};
